workflows:
  ios-app-store:
    name: ParkSafe — App Store
    max_build_duration: 60
    instance_type: mac_mini_m2

    integrations:
      app_store_connect: App Store Connect API Key

    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.rambitllc.parksafe
      vars:
        XCODE_PROJECT: "ParkSafeSF/ParkSafeSF.xcodeproj"
        XCODE_SCHEME: "ParkSafeSF"
        BUNDLE_ID: "com.rambitllc.parksafe"
        APPLE_TEAM_ID: "8R3A53R86P"
        APP_STORE_ISSUER_ID: "84975076-bb2f-40b1-a26e-8b515e393aeb"
        APP_STORE_KEY_ID: "RZ8H98NB94"
        APP_STORE_PRIVATE_KEY: |
          -----BEGIN PRIVATE KEY-----
          MIGTAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBHkwdwIBAQQgYj9Icx8E78Mw+gJr
          YO6HT/X0ATMUyHO56iR1A86cxg6gCgYIKoZIzj0DAQehRANCAAT87Z75wcnmHXeH
          MKNrMa8zjk31loyBVuvYEYjo9dKBNftvCAHy7d6FTLDB9nCLYGivFkn3sAWDCXnV
          IouOpj47
          -----END PRIVATE KEY-----
      xcode: latest

    scripts:
      - name: Unzip project
        script: |
          if [ -f "ParkSafe.zip" ]; then
            unzip -o ParkSafe.zip
            echo "Unzipped ParkSafe.zip"
          elif [ -f "ParkSafeSF.zip" ]; then
            unzip -o ParkSafeSF.zip
            echo "Unzipped ParkSafeSF.zip"
          else
            ls -la
          fi

      - name: Build and archive
        script: |
          xcodebuild \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath "$CM_BUILD_DIR/ParkSafe.xcarchive" \
            clean archive \
            DEVELOPMENT_TEAM="$APPLE_TEAM_ID" \
            PRODUCT_BUNDLE_IDENTIFIER="$BUNDLE_ID" \
            CODE_SIGN_STYLE=Automatic \
            -allowProvisioningUpdates \
            -allowProvisioningDeviceRegistration

      - name: Export IPA
        script: |
          xcodebuild \
            -exportArchive \
            -archivePath "$CM_BUILD_DIR/ParkSafe.xcarchive" \
            -exportPath "$CM_EXPORT_DIR" \
            -allowProvisioningUpdates \
            -exportOptionsPlist /dev/stdin <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store-connect</string>
            <key>teamID</key>
            <string>$APPLE_TEAM_ID</string>
            <key>signingStyle</key>
            <string>automatic</string>
            <key>uploadSymbols</key>
            <true/>
            <key>compileBitcode</key>
            <false/>
          </dict>
          </plist>
          EOF

    artifacts:
      - $CM_EXPORT_DIR/*.ipa
      - $CM_BUILD_DIR/*.xcarchive

    publishing:
      app_store_connect:
        api_key: $APP_STORE_PRIVATE_KEY
        key_id: $APP_STORE_KEY_ID
        issuer_id: $APP_STORE_ISSUER_ID
        submit_to_testflight: false
        submit_to_app_store: false
